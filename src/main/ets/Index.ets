/**
 * Native entry re-export.
 *
 * The shared library itself is a NAPI module (libreader_enhancement.so).
 * Consumers import it directly by name:
 *   import nativeEnhancement from 'libreader_enhancement.so'
 */
export {}
